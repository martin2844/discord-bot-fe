const BookId = ({ params }: { params: { bookId: string } }) => {
   return (
      <>
         <div>
            {params.bookId}
            <br />
            asdasdads
         </div>
      </>
   );
};

export default BookId;
